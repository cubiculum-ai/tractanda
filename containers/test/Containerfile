# Swift 6.4 release-branch snapshot, Debian 12; multi-platform index resolved 2026-09-08.
# Source tag: swiftlang/swift:nightly-6.4.x-debian12. Keep the digest pinned between test runs.
FROM docker.io/swiftlang/swift@sha256:8554bb765eac670fb9900a933bd5a695b949bbaabbd388b7a6ba4ad943647efe
# Independent wire clients and a kernel POSIX ACL comparison fixture.
RUN apt-get update && apt-get install -y --no-install-recommends python3 acl libsqlite3-dev libpam0g-dev uuid-dev \
    && rm -rf /var/lib/apt/lists/*
